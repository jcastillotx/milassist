{
  "development": {
    "username": "postgres",
    "password": "postgres",
    "database": "milassist_dev",
    "host": "localhost",
    "dialect": "postgres",
    "pool": {
      "max": 5,
      "min": 1,
      "acquire": 30000,
      "idle": 10000
    }
  },
  "test": {
    "username": "postgres",
    "password": "postgres",
    "database": "milassist_test",
    "host": "localhost",
    "dialect": "postgres",
    "pool": {
      "max": 5,
      "min": 1,
      "acquire": 30000,
      "idle": 10000
    }
  },
  "production": {
    "use_env_variable": "DATABASE_URL",
    "dialect": "postgres",
    "dialectOptions": {
      "ssl": {
        "require": true,
        "rejectUnauthorized": false
      }
    },
    "pool": {
      "max": 10,
      "min": 2,
      "acquire": 30000,
      "idle": 10000
    }
  }
}
